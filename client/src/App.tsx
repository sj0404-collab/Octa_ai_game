/**
 * Prism Relay design reminder: React is only the frame; the full-screen game canvas is the experience.
 * Keep the presentation sparse, electric, and instrument-like—never turn the route into a conventional website.
 */
import GameCanvas from "@/components/GameCanvas";

export default function App() {
  return <GameCanvas />;
}
